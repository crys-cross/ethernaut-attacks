import { ethers } from "hardhat";

// const lostAddress =  keccack256(address, nonce);

// const functionSignature = {
//     name: 'destroy',
//     type: 'function',
//     inputs: [
//         {
//             type: 'address',
//             name: '_to'
//         }
//     ]
// };

// const params = [player];

// const data = web3.eth.abi.encodeFunctionCall(functionSignature, params);

// const await web3.eth.sendTransaction({from: player, to: tokenAddr, data});
